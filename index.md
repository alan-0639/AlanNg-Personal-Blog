---
layout: home

hero:
  name: "Alan Ng 個人博客"
  text: ""
  tagline: 客制化機械鍵盤作品
  image:
    src: /個人卡通頭像.png
    alt: Alan Ng
  actions:
    - theme: brand
      text: 創作歷程
      link: '/zh-HK/HAWKZ-dim-sum-102/PCB設計'    
    - theme: brand
      text: 作品功能
      link: '/zh-HK/默认配置/'
    - theme: brand
      text: 視頻展示
      link: '/zh-HK/視頻/焊接'


# 关键：禁用默认的 features 渲染
features: []
---

<!-- 自定义 Features 区块开始 -->
<div class="custom-features">
  <div class="features-grid">
    <!-- 1. 从0到1项目落地 -->
    <div class="feature-card">
      <h3>🧠從0到1專案落地</h3>
      <div class="feature-tagline">概念 → 設計 → 驗證 → 成品</div>
      <div class="feature-desc">
        獨立完成機械鍵盤專案：概念DFA、原理圖、PCB Layout、韌體編寫、3D建模、對接工廠（CNC/3D列印/鈑金）、整機FVT測試等。
      </div>
      <div class="tech-tags">
        <span>DFM/DFT/DFA</span>
        <span>FVT</span>
        <span>完整閉環</span>
      </div>
    </div>
<!-- 2. PCB设计 & 硬件实现 -->
    <div class="feature-card">
      <h3>🔧PCB設計 & 硬體實作</h3>
      <div class="feature-tagline">原理圖 · PCB · 焊接 · 調試</div>
      <div class="feature-desc">
        雙層PCB Layout、嘉立創EDA、PCB打樣、手工焊接（電烙鐵/風槍/加熱台）。搭配J-Link/ST-Link、萬用表進行PCBA測試除錯。
      </div>
      <div class="tech-tags">
        <span>EasyEDA Pro</span>
        <span>BL/FW燒錄</span>
        <span>焊接實操</span>
      </div>
    </div>
<!-- 3. 结构与机构整合 -->
    <div class="feature-card">
      <h3>🧱結構 & 機械整合</h3>
      <div class="feature-tagline">3D建模 · 2D圖 · 公差分析 · 工廠對接</div>
      <div class="feature-desc">
        Fusion360 3D建模、AutoCAD 2D出圖，結構DFA分析。對接CNC、3D列印、鈑金廠，確保PCB與外殼精準匹配，降低組裝干涉。
      </div>
      <div class="tech-tags">
        <span>Fusion360</span>
        <span>AutoCAD</span>
        <span>DFA</span>
      </div>
    </div>
  </div>
</div>

##
# 🛠 主要應用工具

<!-- 新布局：左图 + 右 2x2 卡片网格 -->
<div class="tool-layout">
  <!-- 左侧大图 -->
  <div class="tool-left-big">
    <img src="/焊接工具.png" alt="主要工具概览">
  </div>

  <!-- 右侧 2x2 卡片网格 -->
  <div class="tool-right-grid">
    <!-- 卡片 1 -->
    <div class="tool-card">
      <div class="tool-card-top">
        <img src="/Github.png" alt="Github 截图">
      </div>
      <div class="tool-card-bottom">
        <img src="/GitHub software.png" alt="Github icon" class="tool-icon">
        <p class="tool-text">Github<br>代碼託管平臺</p>
      </div>
    </div>
    <!-- 卡片 2 -->
    <div class="tool-card">
      <div class="tool-card-top">
        <img src="/嘉立創EDA.png" alt="嘉立創EDA 截图">
      </div>
      <div class="tool-card-bottom">
        <img src="/嘉立创EDA(专业版).png" alt="嘉立創EDA icon" class="tool-icon">
        <p class="tool-text">EasyEDA PRO<br>PCB設計軟件</p>
      </div>
    </div>
    <!-- 卡片 3 -->
    <div class="tool-card">
      <div class="tool-card-top">
        <img src="/QMK代碼.png" alt="QMK 代码截图">
      </div>
      <div class="tool-card-bottom">
        <img src="/Visual Studio Code.png" alt="VS Code icon" class="tool-icon">
        <p class="tool-text">VS Code<br>代碼內容編輯器</p>
      </div>
    </div>
    <!-- 卡片 4 -->
    <div class="tool-card">
      <div class="tool-card-top">
        <img src="/102整機截面分析.png" alt="Fusion360 截图">
      </div>
      <div class="tool-card-bottom">
        <img src="/Autodesk Fusion.png" alt="Fusion360 icon" class="tool-icon">
        <p class="tool-text">Fusion360<br>3D建模軟件</p>
      </div>
    </div>
  </div>
</div>



# 🛠 其他應用工具

<div class="Stool-grid">
  <div class="Stool-item">
    <img src="/KeyShot Studio.png" alt="Github">
    <p><br></p>
  </div>
  <div class="Stool-item">
    <img src="/AutoCAD 2023.png" alt="嘉立創EDA原理圖">
    <p><br></p>
  </div>
  <div class="Stool-item">
    <img src="/Adobe Photoshop 2025.png" alt="QMK代碼">
    <p><br></p>
  </div>
  <div class="Stool-item">
    <img src="/STM32 ST-LINK Utility.png" alt="102整機截面分析">
    <p><br></p>
  </div>
  <div class="Stool-item">
    <img src="/nRF Connect for Desktop.png" alt="102整機截面分析">
    <p><br></p>
  </div>
  <div class="Stool-item">
    <img src="/串口设置5.13.1.png" alt="102整機截面分析">
    <p><br></p>
  </div>
  <div class="Stool-item">
    <img src="/Git Bash.png" alt="102整機截面分析">
    <p><br></p>
  </div>
  <div class="Stool-item">
    <img src="/J-Link RTT Viewer V7.94i.png" alt="102整機截面分析">
    <p><br></p>
  </div>
  <div class="Stool-item">
    <img src="/仿真电路模拟器.png" alt="102整機截面分析">
    <p><br></p>
  </div>
  <div class="Stool-item">
    <img src="/QMK MSYS.png" alt="QMK MSYS">
    <p><br></p>
  </div>


</div>


##
# 📜 部分證書展示

<div class="cert-grid">
  <div class="cert-item">
    <img src="/IMG_4864(20260530-064118).png" alt="大專畢業證">
    <p>大專畢業證<br>（中國學信網認證）</p>
  </div>
  <div class="cert-item">
    <img src="/嘉立創pcb證書.png" alt="PCB設計專項技術證">
    <p>PCB設計專項技術證書電子檔<br>（嘉立創集團特批證書）</p>
  </div>
  <div class="cert-item">
    <img src="/中國電子學會PCB證書.png" alt="PCB設計專項技術證">
    <p>PCB設計專項技術證書<br>（中國電子學會認證）</p>
  </div>
    <div class="cert-item">
    <img src="/高新計算機技術證書.png" alt="高新計算機技術證書">
    <p>高新計算機技術證書<br>（教育部認證）</p>
  </div>
</div>

<style>


<!-- ========== 样式定义（自适应亮色/暗色主题，移动端友好） ========== -->
<style>


/* ------------------------------ 大卡片 样式 ------------------------------ */
.custom-features {
  margin: 2rem 0;          /* 上下外边距，与上下内容保持距离 */
}


/* 使用 CSS Grid 布局实现响应式卡片网格 */

.features-grid {
  display: grid;
  grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
  gap: 2rem;               /* 卡片之间的间隙 */
}

/* 单个卡片样式 */
.feature-card {
  background: var(--vp-c-bg-soft);   /* VitePress 主题变量：柔和背景色（暗色模式自动适配） */
  border-radius: 1.5rem;             /* 圆角 */

  padding-top: 0.5rem;  /* 上边距*/
  padding-bottom: 1rem;  /* 下边距*/
  padding-left: 1.5rem;  /* 左边距*/
  padding-right: 1.5rem;  /* 右边距*/

  transition: transform 0.2s, box-shadow 0.2s; /* 平滑过渡效果 */
  border: 2px solid var(--vp-c-divider);       /* 边框颜色使用主题分割线变量 */
  display: flex;
  flex-direction: column;            /* 内部元素垂直排列 */
  height: 100%;                      /* 让同一行卡片高度一致 */
}

/* 鼠标悬停效果：轻微上浮 + 阴影 + 边框变色 */
.feature-card:hover {
  transform: translateY(-5px);
  box-shadow: 0 20px 30px -12px rgba(0, 0, 0, 0.1);
  border-color: var(--vp-c-brand-1); /* 品牌色边框 */
}

/* 卡片标题样式（h3） */
.feature-card h3 {
  font-size: 1.4rem;
  font-weight: 600;

  margin-top: 0.5rem;  /* 上边距*/
  margin-bottom: 0.5rem;  /* 下边距*/

  color: var(--vp-c-text-1);         /* 主题主要文字颜色 */
}

/* 短标语（tagline）样式 */
.feature-tagline {
  font-weight: 500;
  color: var(--vp-c-brand-1);        /* 品牌色文字 */
  font-size: 0.85rem;
  text-transform: uppercase;         /* 大写字母 */
  letter-spacing: 0.5px;
  margin-bottom: 1rem;
  border-left: 3px solid var(--vp-c-brand-1); /* 左侧装饰条 */
  padding-left: 0.6rem;
}

/* 详细描述文字 */
.feature-desc {
  color: var(--vp-c-text-2);         /* 主题次要文字颜色 */
  font-size: 0.95rem;

  margin-top: 0rem;  /* 上边距*/
  margin-bottom: 1rem;  /* 下边距*/

  flex: 1;                           /* 占用剩余空间，使标签底部对齐 */
}

/* 技术标签容器 */
.tech-tags {
  display: flex;
  flex-wrap: wrap;                   /* 小屏幕时换行 */
  gap: 0.5rem;
  margin-top: auto;                  /* 自动上边距，将标签推到底部 */
}

/* 单个小标签样式 */
.tech-tags span {
  background: rgba(0, 0, 0, 0.1);   /* 静音背景色（比普通背景稍暗/稍亮） */
  padding: 0.2rem 1rem;
  border-radius: 0.75rem;               /* 胶囊形状 */
  font-size: 0.7rem;
  font-weight: 1000;
  color: var(--vp-c-text-2);  
}





/* ------------------------------ 证书网格样式------------------------------ */
.cert-grid {
  display: grid;
  grid-template-columns: repeat(auto-fill, minmax(240px, 1fr));
  gap: 1.5rem;
  margin: 2rem 0;
}

/* 单个证书卡片 */
.cert-item {
  background: var(--vp-c-bg-soft);
  border-radius: 12px;
  padding: 0.5rem;
  text-align: center;
  transition: transform 0.2s;
  border: 5px solid var(--vp-c-divider);
  height: 350px;                     /* 固定高度，保证卡片整齐 */
  display: flex;
  flex-direction: column;
  justify-content: space-between;    /* 图片靠上，文字靠下 */
}

/* 证书卡片悬停效果 */
.cert-item:hover {
  transform: translateY(-8px);
}

/* 证书图片样式 */
.cert-item img {
  display: block;
  margin:  auto;                    /* 水平居中 */
  max-width: 100%;
  height: 80%;                       /* 占卡片高度的70% */
  max-height: 300px;
  object-fit: contain;               /* 保持图片比例，不裁剪 */
  border-radius: 8px;
  box-shadow: 0 2px 8px rgba(0, 0, 0, 0.1);
}

/* 证书文字说明 */
.cert-item p {
  margin: 0.75rem 0 0;
  font-weight: 500;
  color: var(--vp-c-text-1);
}

/* ---------- 响应式：小屏幕（手机）时卡片内边距减小 ---------- */
@media (max-width: 680px) {
  .features-grid {
    grid-template-columns: 1fr;      /* 手机屏幕每行只显示一张卡片 */
  }
  .feature-card {
    padding: 1.5rem;
  }
}

/* ------------------------------ 主要應用工具样式 ------------------------------ */

/* ============================================================
   主要應用工具样式（左侧大图 + 右侧 2×2 卡片网格）
   此部分样式用于“主要應用工具”区块，实现自适应布局
   ============================================================ */

/* ----- 整体布局容器（左图 + 右网格） ----- */
.tool-layout {
  display: flex;              /* 弹性盒模型，使左右并排 */
  gap: 2rem;                 /* 左右之间的间距 */
  margin: 2rem 0;            /* 上下外边距，与上下内容隔开 */
  align-items: stretch;      /* 让左右两侧高度自动拉伸到相同高度（默认是拉伸） */
}

/* ----- 左侧大图容器 ----- */
.tool-left-big {
  flex: 1;                   /* 宽度占比（相对于右侧 flex:1.5，左侧占 1/(1+1.5)=40% 左右） */
  min-width: 200px;          /* 最小宽度，防止过窄时内容被挤压 */
  background: var(--vp-c-bg-soft);  /* 使用 VitePress 主题变量：柔和背景（暗/亮自动适配） */
  border-radius: 12px;       /* 圆角 */
  border: 5px solid var(--vp-c-divider); /* 边框，使用主题分割线颜色 */
  overflow: hidden;          /* 裁剪溢出内容，确保圆角生效 */
  display: flex;             /* 内部弹性布局，用于居中图片 */
  align-items: center;       /* 垂直居中图片 */
  justify-content: center;   /* 水平居中图片 */
}

/* 左侧大图内部的图片 */
.tool-left-big img {
  width: 100%;               /* 图片宽度占满容器 */
  height: 100%;              /* 图片高度占满容器 */
  object-fit: contain;       /* 保持图片原始比例，完整显示（不裁剪），可能产生留白 */
  display: block;            /* 移除图片底部空隙（默认是 inline-block） */
  
}

/* ----- 右侧 2×2 卡片网格容器 ----- */
.tool-right-grid {
  flex: 1.5;                 /* 宽度占比（右侧占 1.5/(1+1.5)=60%），比左侧宽，更突出卡片 */
  display: grid;             /* 使用网格布局 */
  grid-template-columns: 1fr 1fr;  /* 固定两列，每列等宽 */
  grid-template-rows: 1fr 1fr;     /* 固定两行，每行等高（高度由父容器拉伸决定） */
  gap: 1.5rem;              /* 网格单元之间的间距（行和列都适用） */
}

/* ============================================================
   单个工具卡片样式（用于右侧 2×2 网格）
   设计目标：上方大图自动填充剩余空间，底部行（小图标+文字）固定高度，
             确保在任何窗口宽度下都不会产生多余留白。
   ============================================================ */

/* ----- 卡片容器（整体） ----- */
.tool-card {
  background: var(--vp-c-bg-soft);   /* 使用 VitePress 主题的柔和背景色（暗/亮模式自动适配） */
  border-radius: 12px;               /* 卡片圆角 */
  border: 5px solid var(--vp-c-divider); /* 边框，颜色取自主题分割线变量 */
  padding: 1rem;                     /* 内边距：上下左右均为 1rem，使内容不贴边 */
  display: flex;                     /* 启用弹性布局，让内部元素可灵活排列 */
  flex-direction: column;            /* 垂直排列（上 → 下），即大图在上，底部行在下 */
  transition: transform 0.2s;        /* 悬停动画平滑过渡（0.2秒） */
  overflow: hidden;                  /* 裁剪溢出部分，确保圆角能正常显示（尤其是子元素的圆角） */
}

/* ----- 卡片悬停效果 ----- */
.tool-card:hover {
  transform: translateY(-5px);      /* 悬停时向上移动 5px，产生“浮起”的视觉反馈 */
}

/* ----- 卡片上方大图区域（核心自适应部分） ----- */
.tool-card-top {
  flex: 1;                           /* 关键属性：占据除底部行之外的所有剩余垂直空间。
                                         当卡片高度被网格（grid）强制拉伸时，大图区域会自动填满，
                                         而底部行保持固定高度，从而避免底部出现“悬空留白”。 */
  width: 100%;                       /* 宽度撑满卡片 */
  position: relative;                /* 相对定位，为内部绝对定位的图片提供定位参考 */
  overflow: hidden;                  /* 裁剪溢出（保证图片圆角不溢出） */
  border-radius: 8px;                /* 大图区域自身圆角，与卡片风格统一 */
  background: var(--vp-c-bg);        /* 图片背景色（通常与主题背景一致，图片加载时可见） */
  min-height: 80px;                  /* 最小高度限制：防止大图区域过小（例如窗口极窄时），保证可读性 */
}

/* ----- 大图区域内的图片（绝对定位填满） ----- */
.tool-card-top img {
  position: absolute;                /* 脱离文档流，由父容器（.tool-card-top）定位 */
  top: 0;                            /* 贴左上角 */
  left: 0;
  width: 100%;                       /* 宽高均为 100%，填满父容器 */
  height: 100%;
  object-fit: contain;               /* 保持图片原始宽高比，完整显示（可能产生上下或左右留白），
                                         但不裁剪图片，确保全部可见 */
  border-radius: 8px;                /* 与父容器圆角一致，让图片也带圆角 */
}

/* ----- 卡片底部行：小图标 + 文字（水平排列） ----- */
.tool-card-bottom {
  display: flex;                     /* 启用弹性布局，让图标和文字水平排列 */
  align-items: center;               /* 垂直居中对齐图标和文字（图标高度与文字行高不同时保持居中） */
  gap: 12px;                         /* 图标与文字之间的间距 */
  margin-top: 0px;                  /* 与大图区域保持间距，让视觉上有呼吸感 */
  flex-shrink: 0;                    /* 关键属性：防止底部行被压缩。
                                         当卡片高度不足时，flex 会优先压缩可伸缩的项（这里是 .tool-card-top），
                                         而底部行固定不缩小，保证图标和文字始终完整显示。 */
}

/* 底部小图标 */
.tool-icon {
  width: 60px;               /* 固定宽度 */
  height: 50px;              /* 固定高度（与宽度不同，可根据需要调整） */
  flex-shrink: 0;            /* 不允许收缩，保持图标固定大小 */
  object-fit: contain;       /* 保持比例，完整显示 */
  border-radius: 8px;        /* 圆角，与整体风格一致 */
}

/* 底部文字 */
.tool-text {
  margin: 0;                 /* 去除默认段落边距 */
  font-size: 0.85rem;        /* 相对单位，较小字号 */
  line-height: 1.4;          /* 行高，多行时阅读舒适 */
  font-weight: 500;          /* 中等粗细 */
  color: var(--vp-c-text-1); /* 使用主题主文字颜色 */
  text-align: left;          /* 左对齐（默认） */
  flex: 1;                   /* 占据剩余空间，使文字撑满右侧 */
}

/* ============================================================
   响应式设计：针对不同屏幕尺寸进行调整
   ============================================================ */

/* 小屏幕（平板或手机横屏，宽度 ≤ 680px） */
@media (max-width: 680px) {
  .tool-layout {
    flex-direction: column;   /* 左右改为上下排列：大图在上，卡片网格在下 */
  }
  .tool-left-big {
    min-height: 200px;        /* 保证大图区域有一定高度，避免太扁 */
  }
  .tool-right-grid {
    grid-template-columns: 1fr 1fr; /* 卡片仍保持两列，节省垂直空间 */
  }
}

/* 极窄屏幕（小屏手机，宽度 ≤ 480px） */
@media (max-width: 480px) {
  .tool-right-grid {
    grid-template-columns: 1fr;   /* 卡片改为单列，垂直堆叠，更易阅读 */
  }
}

/* ============================================================
   以下为旧版样式残留（可能是之前 .tool-grid 的响应式）
   如果已完全迁移到新布局，此部分可安全删除
   ============================================================ */
@media (max-width: 680px) {
  /* 注意：这里引用了旧类 .tool-grid 和 .tool-item，若已弃用则无需保留 */
  .tool-grid {
    grid-template-columns: 1fr;   /* 手机每行一个卡片 */
  }
  .tool-item {
    height: auto;                 /* 手机取消固定高度，让内容撑开 */
  }
}


/* ------------------------------ 其他應用工具样式 ------------------------------ */
.Stool-grid {
  display: grid;
  grid-template-columns: repeat(auto-fill, minmax(93px, 1fr));
  gap: 1.5rem;
  margin: 2rem 0;
}

/* 单个证书卡片 */
.Stool-item {
  background: var(--vp-c-bg-soft);
  border-radius: 12px;
  padding: 0rem;
  text-align: center;
  transition: transform 0.2s;
  border: 5px solid var(--vp-c-divider);
  height: 100px;                     /* 固定高度，保证卡片整齐 */
  width: 90px;                     /* 固定高度，保证卡片整齐 */
  display: flex;
  flex-direction: column;
  justify-content: space-between;    /* 图片靠上，文字靠下 */
}

/* 证书卡片悬停效果 */
.Stool-item:hover {
  transform: translateY(-8px);
}

/* 证书图片样式 */
.Stool-item img {
  display: block;
  margin:  auto;                    /* 水平居中 */
  max-width: 100%;
  height: 100%;                       /* 占卡片高度的70% */
  max-height: 300px;
  object-fit: contain;               /* 保持图片比例，不裁剪 */
  border-radius: 8px;
  box-shadow: 0 2px 8px rgba(0, 0, 0, 0.1);
}

/* 证书文字说明 */
.Stool-item p {
  margin: 0.75rem 0 0;
  font-weight: 500;
  color: var(--vp-c-text-1);
}

/* ---------- 响应式：小屏幕（手机）时卡片内边距减小 ---------- */
@media (max-width: 680px) {
  .features-grid {
    grid-template-columns: 1fr;      /* 手机屏幕每行只显示一张卡片 */
  }
  .feature-Stool {
    padding: 1.5rem;
  }
}



</style>


